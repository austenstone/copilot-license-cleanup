name: Usage
on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  run:
    name: Run Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          github-token: ${{ secrets.TOKEN }}
          organization: 'octoaustenstone'
          remove: true
          inactive-days: 30